{
  "name": "elias-app",
  "version": "1.0.0",
  "type": "module",
  "description": "Elias companion app — Live2D + Widget (Capacitor)",
  "scripts": {
    "dev": "npx cap sync && npx cap open android",
    "build": "npx cap sync",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.1",
    "@capacitor/core": "^7.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.1",
    "typescript": "^6.0.3"
  }
}
